<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black_true"
    android:fitsSystemWindows="false">

    <!--
        FragmentContainerView — area utama untuk Library, Folders, Playlists.
        Bottom padding = tinggi NowPlayingBar + BottomNav agar konten
        tidak tertutup kedua view tersebut.
    -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="128dp"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph"/>

    <!--
        NowPlayingBar — mini player, selalu tampil di atas BottomNav.
        Posisi: di atas BottomNavigationView.
        Tinggi: 64dp.
        Visibility: GONE saat tidak ada lagu, VISIBLE saat ada lagu.
    -->
    <include
        android:id="@+id/now_playing_bar"
        layout="@layout/layout_now_playing_bar"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="64dp"/>

    <!--
        BottomNavigationView — navigasi utama.
        Posisi: paling bawah layar.
        Background: true black AMOLED.
    -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_gravity="bottom"
        android:background="@color/black_true"
        app:itemIconTint="@color/selector_nav_item"
        app:itemTextColor="@color/selector_nav_item"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_bottom_nav"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>